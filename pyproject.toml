[project]
name = "mtnmomo"
version = "0.1.0"
description = "MTN Momo API client for Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.1",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.2.2",
]
